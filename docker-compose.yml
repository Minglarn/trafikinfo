version: '3.8'

services:
  backend:
    image: ghcr.io/minglarn/trafikinfo-backend:latest
    container_name: trafikinfo-backend
    ports:
      - "7000:8000"
    volumes:
      - backend-data:/app
    restart: always
    environment:
      - DATABASE_URL=sqlite:///./trafikinfo.db

  frontend:
    image: ghcr.io/minglarn/trafikinfo-frontend:latest
    container_name: trafikinfo-frontend
    ports:
      - "7080:80"
    depends_on:
      - backend
    restart: always

volumes:
  backend-data:
